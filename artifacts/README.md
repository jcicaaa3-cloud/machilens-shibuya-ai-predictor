# Artifacts

This folder contains a small demo checkpoint for portfolio review.

The checkpoint is trained on synthetic data and is not an official forecasting model. It exists so the prediction script can run without requiring a long training step first.
