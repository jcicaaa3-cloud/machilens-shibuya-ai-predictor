# Data folder

This public package uses synthetic sample data only.

`data/synthetic/synthetic_machilens_samples.csv` is generated by the project code and is included so reviewers can inspect the shape of the pipeline. It is not copied from an external dataset and should not be interpreted as official Shibuya data.

Raw-data folders are intentionally empty placeholders.
